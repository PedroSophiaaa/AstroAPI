import json as js
import pandas as pd
import numpy as np
import seaborn as sns

# importando as bibliotecas necessárias para a execução dos códigos.

data1 = pd.read_json(r'C:\JupyterLab\Machine learning\Dados_Real\data-80-(32_16867469879518).json')
print(data1)

# teste do "pandas" lendo "json".

lista_lat = np.linspace(90, -90, 250)
print(lista_lat)

# lista com as latitudes.

data2 = {}
for lat in range(len(lista_lat)):
    data2[lat] = pd.read_json(fr'C:\JupyterLab\Machine learning\Dados_Real\data-{lat}-({str(lista_lat[lat]).replace(".", "_")}).json')

print(data2)

# criação de um dicionário contendo todos os dados coletados.

termos_importantes = ["temperature", "pressure", "feelslike", "weather_descriptions", "wind_speed", "wind_degree", "precip", "humidity", "cloudclover", "uv_index", "visibility", "is_day"]

df = pd.DataFrame()

for lat in range(len(lista_lat)):
    dicio = {}
    dicio[lat] = pd.read_json(fr'C:\JupyterLab\Machine learning\Dados_Real\data-{lat}-({str(lista_lat[lat]).replace(".", "_")}).json')
    df = df.append(dicio, ignore_index=True)
    
df[10]